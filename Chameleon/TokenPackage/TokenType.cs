namespace Chameleon.TokenPackage
{
    public enum TokenType
    {
        IDENTIFIER, NUMBER, STRING, LABEL,
        OPERATOR, COMP_OPERATOR, ASSIGN, SPE_OPERATOR,
        LEFT_P, RIGHT_P, LINE, UNKNOWN, NULL, EOF
    }
}
